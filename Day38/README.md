# Day 38: Python Scripting in Cloud 🐍

## The Mission
- Transitioned from "Static Hosting" (HTML) to "Dynamic Computing" (Python).
- Verified Python installation on Ubuntu.

## The Code
- Created `logic.py` to simulate server load calculation.
- Used `random` library and `if/else` logic.

## Execution
- **Command:** `python3 logic.py`
- **Result:** Server calculated `15 + 40 = 55%` load and triggered a "High Load" warning.
